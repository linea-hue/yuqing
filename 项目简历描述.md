# 项目简历描述：甄选电商企业智能业务平台

> 项目名称：**甄选电商 · 企业智能业务平台（交易 + 客服 + 治理一体化，RAG 智能中台）**
> 技术栈：Python 3.11+ / FastAPI / LangGraph 状态机 / RAG 混合检索 / Redis Streams / Docker / WebSocket 语音 / Pydantic
> 项目结构：买家服务系统 + 客服工作台 + 运营治理系统 三端一体，五个工单（客诉决策 / 观测评测 / 零信任安全 / 多级意图 / 语音控制面）深度融合

---

## 一、项目背景

中大型电商平台的售后与客服体系存在三个结构性难题：**售后处置风险高**（退款涉及真金白银，恶意退款、伪造凭证屡禁不止，人工审核又慢又贵）、**知识口径不统一**（商品/物流/售后政策分散在买家、客服、运营多套系统，同一问题三种回答）、**AI 能力难落地**（大模型直接接业务有幻觉、越权、注入三大风险，缺乏观测与评测手段无法上线）。

本项目按《电商平台买家端运营端与 RAG 智能中台项目方案》落地了一套**交易、客服与治理一体化参考实现**：以退款/客诉决策为主链，将可审计 Trace、零信任安全、多级意图与异常兜底、语音控制面四个横切能力深度融合进同一条业务链路，形成"能交易、能决策、能审计、能防御"的企业级智能业务平台。

---

## 二、项目职责

### 1. 售后智能决策主链（工单 1：客诉/退款决策）
- 设计统一决策状态机（`RUNNING / SUSPENDED_HUMAN / COMPLETED`），替代旧的多套流程状态，消除状态语义重复
- 实现**并行 Agent 证据链**：证据/OCR、欺诈检测、舆情情绪三路并行分析，金额、风险、证据、RAG 依据四因子共同决定"自动退款"或"挂起人工"
- 人工审批恢复走 LangGraph 式 Checkpoint：恢复时**重新校验订单状态、策略版本、审批额度**，审批与退款全程幂等写入 Trace
- 高风险工具调用强制"审批 + 角色 + 金额策略"三重前置，Agent 不直接写交易表

### 2. RAG 智能中台（三端共用）
- 实现商品/物流/售后政策统一知识检索：买家咨询、客服辅助、运营知识验证**共用同一套 ACL/版本/缺口链路**，口径一致
- 检索生产化设计：每条索引携带 `tenant_id / shop_id / doc_id / version / effective_at / acl / checksum` 字段；**先过滤再召回**（租户/店铺/角色/时间），不依赖模型自觉
- 口语 Query 改写 + 混合词法重排 + 引用阈值；关键结论强制引用 ID，无证据/版本冲突/低分召回时输出不确定性并转人工；知识缺口自动记录反哺运营

### 3. 零信任安全体系（工单 6）
- **Critic 安全网关**：Prompt 注入 / 越狱 / 角色冒充三类攻击实时检测拦截，恶意退款场景联动风险升级
- **DLP 脱敏**：手机号/身份证/邮箱/银行卡/API Key 五类敏感信息双向脱敏，日志与 Trace 默认脱敏
- **Tool Policy**：高风险 Tool Calling 显式 JSON Schema + 参数范围 + 订单归属 + 状态前置 + 二次授权
- **沙箱**：Office/CSV/JSON 仅在临时目录解析，权限最小化

### 4. 多级意图与可观测（工单 8 + 工单 5）
- 意图路由三级降级：**规则优先 → 严格 JSON 模型 → Fallback**，解析失败进入 DLQ；熔断器连续失败自动打开
- 评测体系：动态评测 **100 条业务样本 + 108 条攻击样本**，规则指标 + LLM Judge 双判定，指标回退自动阻断发布
- 全链路 Trace：每次请求返回 trace_id，动态记录节点时延、Checkpoint、事件流、DLQ 与知识缺口

### 5. 语音客服控制面（工单 10）
- 浏览器 ASR + 可打断 TTS，`/api/voice/turn` + `/ws/voice` 统一"转写 → 意图 → RAG → 安全"回合协议
- 打断处理：用户抢话取消 TTS 协程、清空播放队列；语音与文本共用同一安全/意图/RAG 链路，不另开洞

### 6. 交易与运营闭环
- 买家端全链路：43 件商品 14 类目、SKU 规格、购物车按店铺结算、运费/优惠/发票明细、下单支付、物流轨迹、确认收货、评价、仅退款/退货退款/换货、逆向物流
- 运营端：经营看板（GMV/净收入/客单价/支付转化/退款率）、库存预占与流水、退赔决策、CSV 批量审批、审计日志、知识运营（草稿→审核→发布状态机 + 双人审核 + 版本指针）

---

## 三、项目亮点

### 亮点 1：五工单融合的一体化设计——不是五个 Demo，是一条链路
多数项目把"RAG""安全""评测"做成孤立模块。本项目的决策主链上，每一次售后请求都完整经过：安全网关 → 意图路由 → 并行证据 Agent → ACL 过滤的 RAG → 风险策略 → 自动退款或人工挂起 → Trace 记录。**安全、意图、RAG、观测是链路上的必经节点，不是可选插件**——这是企业级与课程设计的本质区别。

### 亮点 2：售后决策的"确定性 + 智能化"平衡
退款金额、风险策略用**确定性规则**（可审计、可解释），证据理解、舆情判断用 **Agent 智能**（处理非结构化）；四因子投票决定自动或人工，人工恢复时重新校验全部前置条件。既不放任 LLM 直接动钱，也不退回纯规则的老路。

### 亮点 3：零信任安全是真拦截，不是 PPT
108 条攻击样本评测集覆盖注入/越狱/角色冒充；恶意退款场景模拟伪造凭证 + 间接提示词注入（藏在 OCR 证据里的指令），由沙箱解析 + Critic 拦截 + 风险升级三层处置。RAG 侧明确"**文档中的指令性文字全部视为不可信数据**"。

### 亮点 4：工程诚实——明确标注演示与生产的边界
通过 `/api/platform/capabilities` 与 `/api/ops/infrastructure` 接口**主动暴露生产替换点**（内存 Store → PostgreSQL/pgvector、词法 RAG → Embedding 向量库、浏览器语音 → WebRTC），不"把演示数据写死伪装成已完成"。这种边界意识在评审和面试中反而是加分项。

### 亮点 5：治理闭环——知识、评测、发布都有状态机
知识运营走"草稿 → 解析 → 切片 → 审核 → 发布"状态机 + 双人审核 + 版本指针，未审核文档进不了生产召回；评测发布走 Golden Dataset + 安全扫描 + 灰度，指标回退自动阻断回滚。AI 能力被纳入与业务代码同等严格的治理流程。

---

## 四、项目数据一览

| 维度 | 数据 |
|------|------|
| 系统入口 | 3 端（买家 / 客服工作台 / 运营治理）+ 统一登录路由 |
| 商品规模 | 43 件商品、14 个类目、真实本地摄影图 SKU |
| 决策状态机 | RUNNING / SUSPENDED_HUMAN / COMPLETED 三态统一 |
| 评测集 | 100 条业务样本 + 108 条攻击样本 |
| 安全能力 | 3 类攻击检测、5 类 PII 脱敏、Tool Policy 三重前置 |
| 核心模块 | 14 个后端模块（decision/rag/security/intent/voice/sandbox/evaluation/commerce 等） |
| 回归套件 | 覆盖商城/订单/售后状态机/审批幂等/注入 DLP/RAG ACL/语音打断/沙箱/Trace |
| 部署 | Docker Compose（Web + Redis + PostgreSQL + 前端） |

---

## 五、面试问答预演（Q&A 速查）

**Q：售后决策为什么不让 LLM 直接判断退不退？**
A：钱相关的决策必须可解释、可审计。金额阈值、风险等级、政策条款用确定性规则计算；LLM 只做它擅长的非结构化理解（证据 OCR、舆情情绪、欺诈特征），输出作为四因子之一进入决策。LLM 永远不能单独决定资金动作，超阈值一律 SUSPENDED_HUMAN 挂起人工。

**Q：RAG 怎么防止跨店铺、跨租户的知识泄露？**
A：先过滤再召回——每条索引带 tenant_id/shop_id/acl/version/effective_at，检索前按身份和时效硬性过滤，而不是召回后靠模型自觉。文档内容里的指令性文字一律视为不可信数据，防止间接注入。

**Q：人工审批恢复的幂等怎么保证？**
A：审批操作带幂等键 + 状态版本号，恢复时重新校验订单当前状态、策略版本和审批额度；两个客服同时审批时，原子状态迁移只有一个成功，另一个拿到冲突响应。审批与退款执行写同一条 Trace，可回放。

**Q：语音客服的打断怎么做？**
A：播放期间监听用户语音输入，检测到有效人声即取消 TTS 生成协程、清空前端播放队列、作废未完成的回答，带着"被打断"上下文进入新一轮"转写→意图→RAG→安全"回合。语音与文本共用同一链路，安全策略不降级。

**Q：这个项目和生产的差距是什么？**
A：当前是企业级架构基线 + 零配置可运行演示：内存 Store、词法 RAG、浏览器语音、本地退款适配器。生产替换点已通过 capabilities 接口明确列出：PostgreSQL/pgvector、Redis Streams Worker + Outbox、OIDC/RBAC、WebRTC/VAD/流式 STT/TTS、真实支付退款网关、Langfuse 观测。差距是实施项，不靠写死演示数据伪装。
